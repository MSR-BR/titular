# Deck

This directory contains the memorial presentation source.

## Subdirectories

- `content/`: structured slide content, outlines, and future speaker notes.
- `assets/`: approved visual assets for the deck.
- `styles/`: presentation CSS and print/export styles.
- `scripts/`: small client-side scripts for the presentation if needed.

Change 004 replaces the shell content with the first real memorial deck for review.
